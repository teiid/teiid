CREATE FOREIGN TABLE test (
	Type string,
	Name string,
	CustomerID string
) OPTIONS (NAMEINSOURCE '`test`', UPDATABLE TRUE);

CREATE FOREIGN TABLE test_CreditCard (
	CardNumber string,
	Type string,
	CVN integer,
	Expiry string
) OPTIONS (NAMEINSOURCE '`test`.`CreditCard`', UPDATABLE TRUE);

CREATE FOREIGN TABLE test_Items (
	Items object
) OPTIONS (NAMEINSOURCE '`test`.`Items`', UPDATABLE TRUE);

CREATE FOREIGN TABLE test_SavedAddresses (
	SavedAddresses string
) OPTIONS (NAMEINSOURCE '`test`.`SavedAddresses`', UPDATABLE TRUE);